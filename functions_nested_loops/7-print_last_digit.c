#include "main.h"
/**
 * print_last_digit - entry point to prints the last digit of a number
 *
 * Return: the last digit
 *
 */
int print_last_digit(int n)
{
	int i;
	n = i % 10;
	_putchar(n);
	return (0);
}

