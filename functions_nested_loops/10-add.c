#include "main.h"
/**
 * add - entry point to adds two integers and return result
 * @a: character to add with b
 * @b: character to addd with a
 *
 * Return: the summ of the two integers
 *
 */
int add(int a, int b)
{
	return (a + b);
}
