#include <stdio.h>
/**
 * fizz_buzz - entry point
 *
 * Return: numbers and fizz buzz
 *
 */
int main(void)
{
	for ( i = 1; i < 100; i++)
	{
		if ( i % 3 = 0)
		printf('Fizz ');
		else if (i % 5 = 0)
			printf('Buzz ')
		else if (i % 3 = 0 && i % = 0)
			printf('FizzBuzz ')
		else
			printf(i);
	
