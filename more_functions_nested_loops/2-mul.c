#include "main.h"
/**
 * mul - is entry point to multiplication a by b
 * @a: is a character to multiply
 * @b: is a character to multiply
 *
 * Return: the result of the multiplication
 *
 */
int mul(int a, int b)
{
	return (a * b);
}
