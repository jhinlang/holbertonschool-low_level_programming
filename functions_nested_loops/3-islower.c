#include "main.h"
/**
* _islower - entry point for check lowercase character
*
* Return: Always 0 (Success)
*
*/
int _islower(int c)
{
int c;
if (c >= 61 && c <= 122)
	{
	_putchar('1');
else
	_putchar('0');
	}
	_putchar('\n');
	return (0);
}
