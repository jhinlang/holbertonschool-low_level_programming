#include "main.h"
/**
 * _abs - entry point to computes the absolutes value of integer
 * @n: is the character to check
 *
 * Return: the absolute value of n character
 *
 */
int _abs(int n)
{
	if (n <0)
		return (-n);
	else
		return (n);
}	
